# Nimbus handbook

A deliberately un-ZAgentic repository: the map lives under `handbook/`, there is
no `docs/` tree at all, and the architecture pages sit one level deeper than the
skill's own handbook does.

docs-map: handbook/map.md
