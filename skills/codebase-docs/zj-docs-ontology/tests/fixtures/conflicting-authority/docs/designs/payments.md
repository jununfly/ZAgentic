---
doc-kind: design
---
# Payment design

<!-- The governance tool never opens this page: the conflict this fixture proves
     lives in the map's bindings, not in any page's front matter. It also stays
     out of `docs/architecture/` on purpose — a page there would drag this
     fixture under `zj-docs-architecture`'s page contract too. -->
