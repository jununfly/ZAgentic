# Handoff — 2026-08-13 ZAgentic 1 阶段收尾 + 1-6 explore 启动

> **下一会话焦点（用户原始指令）**: 继续 1-6 explore 烤问 (3 子节点独立 vs 合并)，可附带处理 1-2 vs 1-6 id 命名遗留。
> **写于**: 2026-08-13 22:32 GMT+8
> **作者**: 上一会话 agent（commit b35d1ae 时点）

---

## TL;DR

ZAgentic 1 阶段（A↔B skill-pair 对比与处理）已全闭环。**35 节点 roadmap，28 completed + 7 pending (1-6 父 + 1-6-1/2/3 全部 explore 模式待开)**。最后 commit `b35d1ae`。

下一会话建议直接进 1-6-1 (zj-debrief) explore 烤问：独立 skill vs 并入 zj-grilling。其他 2 个子节点（1-6-2 steelman, 1-6-3 dry-run）按相同模板处理。

---

## 当前状态快照

### 工作区
- 干净，无未 commit 改动
- 最后 commit: `b35d1ae` (2026-08-13 22:30 GMT+8)
- 分支: `main`，已同步到 origin

### Roadmap 全貌
- 路径: `docs/plans/roadmap-skillpairs.json` (JSON 唯一真相源)
- md 视图: `docs/plans/roadmap-skillpairs.md`（render 刷出，禁止手动改）
- 累计 35 节点，max_depth 4
- 1 根节点 status: `in_progress`（因 1-6 子非 completed，预期内）

### 1 阶段全闭环
| 父节点 | 状态 | 子数 | 备注 |
|---|---|---|---|
| 1-3 同源对 | completed | 11 + 1-3-3-1 | 含 A-only PR flag 补充段 |
| 1-4 B独有 | completed | 7 | 含 1-4-7 4 合一（wizard/implement/ask-matt/resolving-merge-conflicts） |
| 1-5 A独有 | completed | 4 | sidecar 补齐 |
| 1-6 跨阶段 | pending | 3 子全 explore | **下一会话重点** |

### 1-6 explore 现状（焦点）
| 节点 | 候选 | 互补关系 | 状态 |
|---|---|---|---|
| 1-6-1 | zj-debrief (回顾反思型) | ↔ zj-grilling/grill-me/grill-with-docs (时段: 前/后) | pending |
| 1-6-2 | zj-steelman (自我质疑型) | ↔ zj-grilling/zj-handoff (方向: 反向/正向) | pending |
| 1-6-3 | zj-dry-run (预演型) | ↔ zj-triage/zj-to-spec/zj-to-tickets (时段: 前/中/中后) | pending |

**Q3 元规则** (1-6 父 decision): 候选落地必须满足"对应 A 或 B 已有 skill 的互补关系"，无明确互补点的不立 skill。

---

## A 仓关键约定（接手必读）

### 1. 双平台 sidecar 强制
- **Claude 平台**: SKILL.md frontmatter 加 `disable-model-invocation: true`（user-only skill）
- **Codex 平台**: `agents/openai.yaml` 含 `policy.allow_implicit_invocation: false`
- interface only skill（model 可调）用 openai.yaml 但**不**加 policy 段
- A 仓 37 个 skill 全部 `name=zj-*` 前缀一致（commit 7ee608b 烤问漏点发现：wizard/wait-what 都补了 zj- 前缀）

### 2. zj-git 包装协议（shim 防护）
- WorkBuddy 注入 `genie-safe-delete.cjs` shim 会在 Windows Git Bash 上把 `rm -rf` 真删
- **所有** git 操作（add/commit/push/fetch/pull/stash/rebase/branch/reset）必须用 `./scripts/zj-git` 包装
- 路径: `scripts/zj-git`（1.3KB 跨平台脚本，NODE_OPTIONS=EMPTY 绕过 shim）
- Skill 详见 `skills/engineering/zj-git-bypass-safe-delete/`
- **PowerShell Remove-Item 也不行** — shim 也包了 Node 路径
- 替代方案: `mv <target> <backup>/` 优于 `rm` / `Move-Item`

### 3. Roadmap 操作协议
- **唯一 CLI**: `python "C:/Users/victo/.workbuddy/skills/zj-roadmap-driven/roadmap_cli.py" <cmd>`
- 写类命令需串行（CLI 有 per-roadmap lock）
- **禁止直接 Edit/Read json 文件**——通过 CLI 改
- md section 由 `render` 命令完全重写，手动改会被覆盖
- status 自动同步：**只在 children 结构变更时触发**（add/update --status），单纯 decide 不触发

### 4. PR 标志 3 rule (1-3-3-1 A-only)
落地在 `skills/engineering/zj-triage/A-ONLY.md`（4.3KB）：
- **Rule 1**: skills/ 改动必查 `.codex-plugin/plugin.json` 挂接
- **Rule 2**: git 操作必走 `./scripts/zj-git` 包装
- **Rule 3**: 新术语必先更新 `ZJ-CONTEXT.md` 才合并

PR 漏任一 → wontfix + 走对应 skill 补。

### 5. 词汇表
- `ZJ-CONTEXT.md` (仓根目录, 154 行) — 领域词汇表 (Skills meta / Roadmap / Issue-Triage 三段)
- `docs/zj-adr/0001-*.md` — ADR（已从 `docs/adr/` 迁过来）
- `AGENTS.md` — 仓库约定硬规则（plugin.json 挂接 / bucket 划分）
- `MEMORY.md` (用户级) — shim 根因 + 4 条硬规则 + 检测特征

### 6. A↔B 处理动作统一英文
absorb / adopt / replace / reject / strict-align + **A-only**（A 环境私有化规则）

### 7. 烤问漏点教训（commit 7ee608b 写入 1-4-7-1 decision）
**别让 Agent 编理由, 必须让用户烤问**。如发现 notes 里出现"X 是中性名"等自圆其说的表述, 大概率是 Agent 编的, 立即烤问用户。

---

## 关键文件路径

### Skill 实现
- 37 skill 全部在 `skills/{engineering,productivity,misc,personal}/zj-*/`
- 全部用 `name=zj-*` 一致 (commit 7ee608b 后)
- README 挂接: 顶层 `README.md` + `skills/<bucket>/README.md` + `.codex-plugin/plugin.json`（AGENTS.md 硬约定）

### Roadmap / 计划
- `docs/plans/roadmap-skillpairs.json` — 35 节点 (JSON 唯一真相源)
- `docs/plans/roadmap-skillpairs.md` — 树形 depth=2 + 焦点视图
- `ZJ-CONTEXT.md` — 154 行领域词汇表
- `docs/zj-adr/0001-*.md` — ADR 历史
- `AGENTS.md` — 仓库硬约定

### 脚本 / 工具
- `scripts/zj-git` — git 包装（1.3KB, 跨平台）
- `scripts/check-private-naming.sh` / `link-skills.sh` / `list-skills.sh` / `zagentic-skills-list`
- `.workbuddy/build_roadmap.py` / `render_md.py` — workbuddy 内部

### Memory
- `~/.workbuddy/MEMORY.md` — 跨项目硬规则（shim 根因 + 4 条硬规则 + 检测特征 + 用户级 sub-agent 禁用）
- `.workbuddy/memory/2026-08-13.md` — 今天 250+ 行工作日志
- `.workbuddy/memory/MEMORY.md` — 项目长期 notes

### 提交链（今天 12 commits）
```
3baf87d → 22807da → 9f404a4 → 9b5b8bb → d199867 → 103747c
   → 7227350 → dac53b7 → 6944409 → 7ee608b → 75e2ead → b35d1ae
```
- `75e2ead` — 1 阶段收尾 + 1-3 复查 + 1-6 开启
- `b35d1ae` — 1-6 explore 推进 (3 候选 sub-node)

---

## 烤问模板（1-6-1/2/3 复用）

每个子节点 explore 烤问推荐 3-4 轮（**用 zj-grilling 流程**）：

### 1-6-1 zj-debrief 模板
**Q1 - 落点**:
- A. 独立 skill (zj-debrief/SKILL.md, user-only, disable-model-invocation: true)
- B. 并入 zj-grilling（作为"完工后"时段段, zj-grilling 加 1 段）
- C. 并入 zj-grill-with-docs（grill + 写文档 + 复盘三合一）

**Q2 - 能力边界** (依赖 Q1):
- 走查本次工作的实际路径 vs 计划路径 (drift detection)
- 提取新出现的领域概念/隐喻 (联动 zj-domain-modeling 更新 ZJ-CONTEXT.md)
- 标记"下次会改进的具体动作"
- 触发频率 (完工后 vs 中途暂停 vs 阶段切换)

**Q3 - 触发方式**:
- A. user-only (disable-model-invocation: true) — 完工后用户主动触发
- B. agent-implicit (model 可调) — 完工后 agent 自检
- C. zj-implement 内置 (作为"完工"步骤的强制 flag)

**Q4 = D 模式** (Q4=D 标准化, 1-3 父 decision): 落盘后追加 1 条 ⚠️ 复查项声明。

### 1-6-2 zj-steelman 模板
**Q1 - 落点**:
- A. 独立 skill
- B. flag 合并到 zj-grilling (前置段: steelman → 失败则 grill)
- C. 不立 skill, 仅在 zj-grilling SKILL.md 写 1 段方法论

**Q2 - 与 grilling 关系**:
- A. steelman 是 grilling 的前置 (steelman 强才不 grill)
- B. steelman 是 grilling 的反向并用 (两者都给)
- C. steelman 替代部分 grilling (grill 强度降低)

**Q3 - 触发**: 探索中, 倾向 user-only

### 1-6-3 zj-dry-run 模板
**Q1 - 落点**:
- A. 独立 skill
- B. zj-implement 前置 flag (before commit, run dry-run)
- C. zj-to-tickets 末尾段 (拆完工单后演练)

**Q2 - 模拟粒度**:
- A. 单步推演 (每个 ticket 标卡点)
- B. N 步聚类 (按依赖聚类, 整组推演)
- C. 全链路 (从工单到 merge)

**Q3 - 输出物**:
- 决策瓶颈清单 (给 zj-grilling 精准烤问)
- 工单修订建议 (反推 to-tickets)
- 时间预算 (预计卡多久)

---

## 建议技能（Suggested Skills）

下个会话开场直接调：

1. **`/zj-roadmap-driven`** — 必调, 看 1-6-1 当前状态 + 决策历史
2. **`/zj-grilling`** — 必调, 跑 1-6-1 explore 烤问 (3-4 轮)
3. **`/zj-domain-modeling`** — 1-6-1 落地时联动更新 ZJ-CONTEXT.md (加 "Skill-pair 互补关系" 词条)
4. **`/zj-tdd`** (如需代码) — 烤问决策落盘后写 zj-debrief/SKILL.md
5. **`/zj-implement`** (如需代码) — 实施 zj-debrief skill 文件

**Skill 路径** (按需 load):
- `C:/Users/victo/.workbuddy/skills/zj-roadmap-driven/SKILL.md`
- `C:/Users/victo/.workbuddy/skills/zj-grilling/SKILL.md`
- `C:/Users/victo/.workbuddy/skills/zj-domain-modeling/SKILL.md`

**辅助 skill** (触发条件):
- **`/zj-git-bypass-safe-delete`** — 若 git 异常 (refs 丢失 / .git/ 状态错乱)
- **`/zj-resolving-merge-conflicts`** — 若 commit 期间有冲突

---

## Pending / 未完成项（接手时检查）

| 项 | 状态 | 优先级 |
|---|---|---|
| 1-6-1 zj-debrief explore 烤问 | pending | 高 |
| 1-6-2 zj-steelman explore 烤问 | pending | 中 |
| 1-6-3 zj-dry-run explore 烤问 | pending | 中 |
| 1-2 vs 1-6 id 命名遗留 | follow-up | 低（已记 1-6 decision 备注） |
| 1-3 复查：1-3-10 + 1-3-3 + Q4=D | **已闭环** (commit 75e2ead) | — |
| 1-4 全部 | **已闭环** (commit 7ee608b) | — |
| 1-5 全部 | **已闭环** (commit 6944409) | — |

---

## 工作风格提示

- **commit+push 显式触发** — 用户说 `commit+push` 才提交，不要主动 commit
- **烤问漏点要诚实承认** — 1-4-7 wizard 烤问时 Agent 编了错误理由"wizard 是 B 中性名"，用户问出后修订。教训：notes 出现"X 是中性名"等表述, 大概率是 Agent 编的, 立即让用户烤问
- **每次 commit 后用户给一个反馈** — 用户善于发现漏点（wizard 不加 zj- 前缀就是用户问出），保持烤问开放
- **A↔B 处理动作统一英文** (absorb/adopt/replace/reject/strict-align + A-only) — notes 用这套术语
- **commit 协议用 zj-git** — 永远走 `./scripts/zj-git`，不裸 git
- **commit-msg 文件写 repo 内路径** — `commit-msg-xxx.txt` 先在 repo 内, 用完 `mv /c/tmp/` 不用 `rm`

---

## 引用清单（不重复内容）

- 今天 12 commits 详情 → `.workbuddy/memory/2026-08-13.md` (250+ 行)
- 1-4-7 烤问 6 轮全记录 → `docs/plans/roadmap-skillpairs.json` (id=1-4-7-* decisions 数组)
- A-only PR flag 3 rule 详写 → `skills/engineering/zj-triage/A-ONLY.md`
- 领域词汇表 → `ZJ-CONTEXT.md` (154 行)
- shim 根因 + 4 条硬规则 → `~/.workbuddy/MEMORY.md` (用户级)
- A 仓硬约定 → `AGENTS.md`
- 1-3 父 Q4=D 标准化 decision → `docs/plans/roadmap-skillpairs.json` (id=1-3, decisions 数组末条)
