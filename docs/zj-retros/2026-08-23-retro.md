# Retro — 2026-08-23

## 01:20 — Task: 完成 wayfinder A22 技术方案研究报告实现

**计划基线**: 无 `/zj-grilling` 产出；以 wayfinder A22 的问题、已确认的技术报告契约和前序 A19–A21 实现为基线。

### Drift

跳过无 `/zj-grilling` 基线的 drift 对照。实际完成了 A22 计划内的 brief、质量门、compiler publication 和契约测试；另外发现 receipt 把 brief schema 当作质量门 schema，补充了独立的 `technical-research-quality-gate/v1` schema，并加强了候选证据、card 数量和 comparison claim 约束。

**Drift 判定**: 可接受。新增约束直接修复了自动化消费者可能误读 receipt、以及报告结构不完整却通过检查的风险。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Technical Decision Brief** — 在候选排序前固定用户/job、baseline、目标、约束、阶段、范围和 serious options 的 `technical-decision-brief/v1` 输入；已同步到 `ZJ-CONTEXT.md`。
- **Technical Research Quality Gate** — 在共享 compiler 前校验 brief、sealed ledger 和 `technical-c4/v1` Report IR 的 `technical-research-quality-gate/v1` 机械质量门；已同步到 `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 新增技术报告 publication 字段时，先区分输入 schema、Report IR schema、质量结果 schema，再补 receipt 契约测试。
- ⚠️ 保留低层 compiler 兼容路径时，必须在 public skill 文档中明确它不是新任务的能力边界。

## 08:26 — Task: 完成 wayfinder A23 code-research 质量评测

**计划基线**: A23 ticket；按单 ticket 规则只处理 Repository Map / Architecture Study 质量 fixture 与评测，不进入 A24。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 复用已有评测资产与 Judge 机制 | 发现共享 ZHarness evaluator 将 `reportFamily` 固定为技术报告 family，因此新增 code-research 独立 corpus 和本地质量编排器，保留相同校准指标，不改协议 | 可接受；避免把 code-research 错标成技术报告，也避免扩大协议变更范围 |
| 对现有 Map/Study 做质量验证 | 先发现 Architecture Study secondary claims 被重复追加，修复后将重复 record ID 纳入 hard gate | 可接受；质量契约暴露了真实生成缺陷 |

**Drift 判定**: 可接受。偏差都收敛到 A23 的边界，并提升了 immutable bundle 与 record identity 的可靠性。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Code-research quality fixture corpus** — 与技术报告资产分离的 Repository Map / Architecture Study 受控样本集，annotation 记录必需属性而非 golden prose；已同步到 `ZJ-CONTEXT.md`。
- **Code-research semantic baseline** — 在语义结果可被信任前，记录 Judge 与 human annotation 的分数误差、容差内比例、推荐一致率和风险计数误差；已同步到 `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 新增 research family 时先检查共享 evaluator 的 schema 边界；若协议不支持，新增 owning-skill 评测 seam，不伪装成已有 family。
- ⚠️ 对 bundle 质量 gate，必须同时检查 artifact hash 和跨 shard record ID 唯一性，避免“结构合法但导航歧义”。

## 10:11 — Task: 完成 wayfinder A24 roadmap bundle 性能实现

**计划基线**: A24 ticket；只实现 `zj-roadmap-driven` 的可选 bundle 存储、CLI 兼容、bounded view、迁移与 benchmark，不进入 A25 closeout。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 接入 sharded bundle 并保持命令兼容 | 初版审阅发现新增/删除子树的父状态同步少走一层，已在 CLI 接入前修复并加入契约测试 | 可接受；属于适配器边界内的真实一致性缺陷 |
| bounded tree 只读取请求深度 | 初版 tree 虽不输出深层节点，却提前读取了深层 shard；加入读取计数测试后修复为在边界前停止读取 | 可接受；直接验证了性能承诺而不是只验证输出 |
| 运行全套回归 | `python3` 3.9 无法运行本轮已有的 3.10+ 类型联合代码，改用仓库 `python` 3.13 完成回归；A24 自身保持标准库实现 | 可接受；验证记录明确解释器前提 |

**Drift 判定**: 可接受。偏差都收敛到 A24 的适配器、bounded-read 和验证边界，没有扩大到 A25。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Snapshot interval** — roadmap bundle 在 append-only history 增长过程中创建 materialized snapshot 的事件间隔；低频快照减少小文件增长，同时 current pointer 仍指向最近快照和当前 history sequence；已同步到 `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 新增 storage adapter 时，先用读取计数测试证明 bounded 语义，再扩展 CLI 契约，避免“输出看似 bounded、底层仍全读”。
- ⚠️ 回归命令应在任务开始时记录解释器路径与版本，避免把环境不兼容误判为实现回归。

## 16:32 — Task: 完成 wayfinder A25 研究迁移与 roadmap 验证收口

**计划基线**: A25 ticket；验证研究迁移、公共发现、编译器契约、roadmap bundle 和真实计划案例，完成知识治理收口。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 直接验证 `ZBrainForStudy/docs/plans/` 的 roadmap 迁移 | 该目录实际是 12 个 Markdown 计划文档而非 legacy JSON；因此构造临时等价 JSON fixture，通过公开 migrate/validate/view 路径验证，明确拒绝 Markdown import，并校验外部 corpus hash 不变 | 可接受；避免把 Markdown 误认成事实源 |
| 收口后清理所有 residue | 发现 research compiler artifact 是已有且被 golden contract 使用的本地依赖缓存，未删除；未清理任何外部仓库、分支或 worktree | 可接受；保留可复核现场 |
| 官方 plugin validator 期待每个 bucket 根有 `SKILL.md` | 现行仓库模型是 bucket README + recursive discovery，官方 validator 仍有 4 个已知 bucket-root warning，recursive validation 的 48 skills 全通过 | 可接受；不添加虚假的 bucket skill |

**Drift 判定**: 可接受。A25 保持为验证和知识收口，没有扩展到外部仓库改造、自动迁移或部署发布。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Real plan corpus** — 只读的既有 Markdown 计划文档集合；可派生临时 legacy JSON 做迁移验证，但不能反向导入或写回 Markdown；已同步到 `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 真实案例不是目标格式时，先记录输入事实与适配边界，再设计临时 fixture，避免测试通过却验证了错误的迁移路径。
- ⚠️ closeout 要区分“已知 validator warning”“受控本地 artifact”“用户/外部仓库 residue”，不能用一次清理掩盖三者差异。

## 17:35 — Task: 完成 wayfinder A26 只读路线图存储建议器

**计划基线**: 无 `/zj-grilling` 产出；以 A26 的单 ticket 目标为基线：增加
只读的 `recommend-storage`，提供单 JSON 与 bundle 的采用建议，不自动迁移。

### Drift

跳过无 `/zj-grilling` 基线的 drift 对照。实际实现额外把 bundle 缺失派生索引
不触发重建写入纳入契约，并将 cold bounded-tree/full-section 测量限制在显式
`--measure` 路径。

**Drift 判定**: 可接受。偏差强化了只读边界和性能建议的可解释性，没有扩大到
自动迁移或历史压缩。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Storage recommendation** — `recommend-storage` 输出的版本化只读建议；
  它根据节点/决策规模、事实源字节数和可选本机测量返回保留单 JSON、考虑
  bundle、建议 bundle 或保留 bundle，不修复、迁移或改写路线图；已同步到
  `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 新增“建议”类 CLI 时，先把输出级别、阈值和无副作用保证写成契约，再
  将实际迁移保留为独立显式命令。
- ⚠️ 对可选性能测量，默认路径应保持 bounded/轻量，昂贵的全量测量必须由
  用户显式开启并在输出中标注为本机观测值。

## 18:13 — Task: 完成 wayfinder A27 storage-advisor 阈值校准

**计划基线**: 无 `/zj-grilling` 产出；以 A27 的单 ticket 目标为基线：用真实
路线图样本校准结构信号与性能测量的关系，并拒绝非执行路线图输入。

### Drift

真实样本盘点额外发现 `global-initiative-roadmap.json` 是固定三层 Initiative
Registry 索引，不是 `zj-roadmap-driven` 执行路线图；因此 A27 增加了 root node
校验和拒绝契约，并把两个普通结构信号从 `recommend-bundle` 调整为
`consider-bundle`。

**Drift 判定**: 可接受。偏差直接修复了“错误载体被建议器当成合法路线图”和
“结构压力被误报成当前性能故障”两类风险。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Storage signal** — 路线图指标越过 advisory starting/severe line 的观测；
  structural signal 只说明 artifact pressure，timing signal 才说明本机测得的
  性能条件；已同步到 `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 对真实路线图做性能校准前，先按 schema/根节点区分执行路线图、Registry
  索引和其他 JSON，避免把不同事实模型混进同一个样本集。
- ⚠️ 结构阈值命中只能产生 advisory pressure；只有严重结构信号或显式测量
  达到严重线时，才升级为 `recommend-bundle`。

## 22:15 — Task: 发布 ZAgentic research/roadmap 实现成果

**计划基线**: 无 `/zj-grilling` 产出；发布已完成的 5 个实现提交，并在发布后执行跨阶段收口。

### Drift

跳过无 `/zj-grilling` 基线的 drift 对照。实际先刷新 `origin/main` 并确认严格
可快进（远端落后 5 个提交），随后发布实现提交，再单独执行 debrief，避免把
收口文档混入实现提交。

**Drift 判定**: 可接受。发布范围保持清晰，收口提交与实现提交分离。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Roadmap carrier** — 执行路线图选定的 canonical storage 形式；普通路线图使用
  单 JSON，大型路线图使用显式 bundle，Markdown 只能作为生成视图；已同步到
  `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 发布前先刷新远端并确认 ahead/behind，再执行 main 推送，避免基于过期远端状态发布。
- ⚠️ skill 重构发布后，立即同步实际安装态并跑一个真实消费者验收，确认仓库实现与运行时快照一致。

## 22:39 — Task: A29 真实研究链路 dogfood

**计划基线**: 无 `/zj-grilling` 产出；以 A29 的真实链路目标为基线：
`zj-code-research → zj-research → zj-tech-research-report`，验证 map、study、sealed ledger、technical-c4 报告和质量门闭环。

### Drift

跳过无 `/zj-grilling` 基线的 drift 对照。实际执行中发现两个边界：Architecture Study 的多 entrypoint flow ID 只使用文件路径，导致唯一性 hard gate 失败；未认证 GitHub API 在三仓库采集过程中耗尽额度，编译器把上游错误压缩成了不透明的 `undefined.map` 表象。前者已修复并重建 study，后者没有伪造新 ledger，改用既有成功 sealed ledger 并在 findings/report 中明确其 source brief 与证据边界。

**Drift 判定**: 可接受，但研究 runtime 的 quota/auth 预检和错误诊断需要后续加强。最终 map、study、Report IR、Markdown、HTML 和 receipt 均通过各自质量门；receipt 的 report hash 与 canonical Markdown 一致。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Graph Coordinator** — 协调图执行与委派工作的责任单元，但不自动拥有产品 authority、evidence truth、Human acceptance 或 lifecycle closeout；已同步到 `ZJ-CONTEXT.md`。
- **Unit capability** — 可在 seam 后独立测试、组合和移除的窄能力，不携带源项目的完整产品语义与 ownership；已同步到 `ZJ-CONTEXT.md`。
- **Native semantic owner** — 外部能力组合后仍对 durable facts、责任边界和 lifecycle decisions 保持权威的系统；已同步到 `ZJ-CONTEXT.md`。
- **Adapter-only reuse** — 外部项目只通过可逆 adapter 和 conformance fixture 接入，同时保持 native semantic owner 为事实源的采用模式；已同步到 `ZJ-CONTEXT.md`。

### Actions

- ⚠️ 多仓库研究开始前先做认证与 API quota preflight；额度不足时必须把当前 brief、旧 sealed ledger 和“非本轮 fresh collection”的关系一起落盘。
- ⚠️ 生成式 study record 的稳定 ID 必须包含 source locator 或 evidence identity，并在任何下游编译前运行唯一性 hard gate。

## 23:34 — Task: A31 authenticated live collection acceptance

**计划基线**: 无 `/zj-grilling` 产出；以 wayfinder A31 的单 ticket 目标为基线：用认证 GitHub token 完成真实 fresh collection，并验证 status、sealed ledger、commit pin 和 unknowns。

### Drift

跳过无 `/zj-grilling` 基线的 drift 对照。真实 live 路径首先暴露了 A30 preflight 的标准库调用错误：`urlopen(request, timeout)` 在 Python 3.13 中把浮点数解释为 request body，mock contract 未覆盖这一调用约定。修复为关键字参数并补回归断言后，真实采集成功；没有复用旧 ledger。

**Drift 判定**: 可接受。偏差直接提升了 A30 runtime seam 的真实可用性，并保持 A31 只做 live acceptance 与必要修复。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- 无新领域词汇；本次复用了已有的 **GitHub quota preflight**、**Collection run status** 和 **Sealed ledger**，未扩展 `ZJ-CONTEXT.md` 词汇表。

### Actions

- ⚠️ 外部网络适配器的 mock contract 必须覆盖真实标准库调用签名；完成 mock 回归后，尽早执行一次受控 live smoke test。
- ⚠️ live fresh collection 必须同时保留 status sidecar 与 sealed ledger，并以 status 的 fresh/reuse/blocked 状态作为验收边界。
