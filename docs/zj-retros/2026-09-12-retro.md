# 2026-09-12 retros

## 16:50 — Task: 实现 #81 的 critical-path / impact 两个调度查询（zj-roadmap-driven）

**计划基线**: 无 `/zj-grilling` 产出；基线来自 Issue #81「03 ready / critical-path /
impact 三个调度查询」+ 上游 spec `docs/plans/zj-roadmap-dag-concurrency.md`
（§3 Story #21 / #22）与 `docs/plans/zj-roadmap-execution-graph.md`（#46）。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 补 critical-path + impact（ready 已在 #91/#92 合并） | 达成：两个查询 + 两 carrier 共用模块函数 + 36 条测试 | 无偏差 |
| 只沿 `blocks` 边推导，软边不参与 | 达成；`impact` 有「`informs` 软边不传播」负向控制例 | 无偏差 |
| 文档沿用 #92 的 "Scheduling query" 段，补两项即可 | **偏差**：本地四份文档此前**完全没有**调度查询文档（#92 的 ready 文档只在远端），实际补成完整调度组（含 ready）并加了 `ZJ-CONTEXT.md` 词条 | 工作量扩大；合并时需与 main 已有段落取合并版，已写进 PR body 订正 |
| 等用户在终端 commit+push（沙箱 git 曾不可用） | 用户选 a，本会话代跑：shim 禁用后 git 可用，但仍走 commit-tree plumbing | 流程简化，非技术偏差 |
| 验证基线 251 绿 | 达成（194 discover + 36 调度 + 21 根级），0 failures | 无偏差 |

**Drift 判定**: 可接受。唯一实质偏差是**文档基线判断错误**（以为 #92 的 ready 文档
已在本地），代价是 PR body 里留了一条订正提醒，无返工。

### Concepts

- **Ready set** / **Critical path** / **Impact** — 三个调度查询，已在本次工作中写入
  `ZJ-CONTEXT.md`（Roadmap 段，Blocked chain 之后）。三者均为**只读派生产物**：
  只在读取时沿 `blocks` 边推导 O(V+E)，不落盘、无计数器（计数指标属 Story 24，P3）。

（未新增其它领域术语；实现手法沿用 #80/#82 的既有形状，无新概念。）

### Actions

- ⚠️ **补文档前先 grep 本地文件确认基线**，别假设「已合并的 PR 文档一定在本地」——
  本次误判导致「补一个段」变成「补完整调度组」，且合并时可能与 main 已有段落重复。
- ⚠️ **判断 push 是否成功一律用 `git ls-remote`，不信客户端退出码** —— 本次
  `git push` 返回 exit=1 但服务端已落（`ls-remote` 复核分支 sha 正确），信退出码
  会误判失败并可能重复推送。

---

## 23:15 — Task: zj-git-bypass-safe-delete 补 Symptom H + 发现并修复已装副本漂移

**计划基线**: 无 `/zj-grilling` 产出（用户短指令驱动）；基线 = 上一回合提出的
「把 `safe-bin/rm` 这条补进 SKILL.md 的 Symptom 段」。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 只补一段 Symptom H 文档 | 达成，另加 Prevention 表一行、两处计数词（Four→Five checkpoints / seven→eight symptoms） | 无偏差 |
| 提交后重装 skill 到 `~/.workbuddy/skills/` | **偏差（正向）**：探测发现已装副本落后两个 PR —— SKILL.md 仅 13248 字节、**无 Symptom G/H**、`scripts/` **缺 `disable-safe-delete.ps1`**（#95 才加的） | 重装收益远大于预期，暴露流程漏洞：已装副本长期未同步且无任何校验 |
| 根因核实：`safe-bin/rm` 未被永久修复覆盖 | 用 Grep 核实 `disable-safe-delete.ps1` 只打 4 个文件、不含 `safe-bin/rm`，判断成立 | 无偏差 |

**Drift 判定**: 可接受，但**暴露一个流程洞**：`~/.workbuddy/skills/` 的已装副本可
静默落后数个 PR（WorkBuddy 不报错、技能照常出现在发现列表里），此前无校验环节。

### Concepts

- **Installed-copy drift（已装副本漂移）** — 已写入 `ZJ-CONTEXT.md`（Skills meta
  段）：`~/.workbuddy/skills/<name>/` 的已装副本与仓库源码长期不同步的状态。更新只能
  走「删-重装」（scanner 启动即扫、无 fs.watch），跳过重装时副本静默落后。
- **`safe-bin/rm`（PATH 层 shim 入口）** — 第 5 个 shim 入口，与
  `disable-safe-delete.ps1` 所打的 4 个文件是不同层级：bash 的 PATH 把 `safe-bin/`
  排前，`rm` 直接解析到这个独立脚本，它有自己那套 `dirname` 引导，所以**打完永久修复
  `rm` 照样 exit 127**。已写入该 skill 的 Symptom H，未进 `ZJ-CONTEXT.md`（属环境细节
  而非项目领域语言）。

### Actions

- ⚠️ **更新 / 重装 skill 前先探测已装副本**（对比 SKILL.md 字节数 + `scripts/` 清单），
  别直接 cp 覆盖 —— 本次因此才发现副本缺 `disable-safe-delete.ps1`、落后两个 PR。
- 🔄 **Bash 的 coreutils 可能整体不在 PATH**（`ls` / `head` / `which` 全
  `command not found`，只有 `git` 还能用）：文件操作改用 Python
  （`os.listdir` / `shutil.move|copytree`）；删文件用 `[System.IO.File]::Delete`
  而非 bash `rm`（`safe-bin/rm` 未被永久修复覆盖，且写在 `&&` 链里会静默短路后续命令）。
