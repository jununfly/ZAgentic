# Retro — 2026-08-21

## 23:35 — Task: 完善 zj-tech-design-review 并完成发布前验收

**计划基线**: Wayfinder A1–A6 已闭合；随后用 `zj-roadmap-driven` 实现 validator、fixtures、测试和人工 short-read gate。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 机械质量门完成后进行三类 fixture 验收 | 第一个 fixture 发现 restart 行为缺少阈值；第二个 fixture 首次误读 blocking finding；第三个 fixture 首次误读 owner | 可接受；人工验收发现了只有结构 lint 抓不到的语义可见性问题，并推动 fixture 明确 owner 与阈值 |
| A4 完成后直接 ready to publish | 需要补充 restart 阈值、显式 finding owner，并进行复读 | 需调整；ready 条件应包含人工短读通过，不仅是脚本全绿 |

**Drift 判定**: 可接受且有价值。三次人工反馈都转化为 fixture 或模板的明确性改进。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **Short-read acceptance** — 不看 validator 输出，短读设计稿并恢复结论、blocking finding/owner、下一验证动作/阈值/owner 的人工质量门；已同步到 `ZJ-CONTEXT.md`。
- **Mechanical quality gate** — 只验证结构、状态、证据指针和反例的自动检查，不替代人工判断；已在 `validate_review.py` 和 skill 指南中落地。

### Actions

- ⚠️ 新增 fixture 时，必须把每个 blocking finding 的 owner 和下一验证的阈值写在同一可见区域。
- ⚠️ 把人工 short-read 作为 ready-to-publish 的独立门，不以机械 validator 全绿替代。
