# Retro — 2026-08-24

## 01:02 — Task: ZAgentic research workspace closeout and publish

**计划基线**: 无 `/zj-grilling` 产出 → drift 步骤跳过；以
`zj-neat-freak` 收尾审计和用户确认的清场候选为基线。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 清理临时与过程产物 | 先审计消费者、历史复核价值和生成残留，再分两阶段清场 | 可接受；避免删除仍被测试或文档消费的证据 |
| 保留有用结论 | 将 A29 的稳定历史结论收敛到 Wayfinder 地图，并删除被 A31/A32 替代的过程产物 | 可接受；没有保留第二份现役事实源 |
| 提交并发布收尾结果 | 修复 Git wrapper 执行位，提交清理与文档收敛，并推送到 `origin/main` | 可接受 |

**Drift 判定**: 可接受。当前消费者、历史复核材料和可再生生成物已分开处理；A31/A32 当前研究链路保持完整。

### Concepts

本次没有新增领域词汇，复用了已有的 canonical map、sealed ledger、过程产物和生成残留边界。

### Actions

- ⚠️ 清场前先按“当前消费者 / 历史复核 / 可再生生成物”审计；稳定结论先回收到 canonical 文档，再清理过程产物。

## 01:20 — Task: A34 second real research-combination acceptance

**计划基线**: 无 `/zj-grilling` 产出 → drift 步骤跳过；以 A34 的
steelman/dry-run 边界为基线：第二次真实运行完整
`zj-code-research → zj-research → zj-tech-research-report` 链路。

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 用一次 fresh collection 完成第二次验收 | 第一轮 fresh collection 证据集中在单一 README，保留后又执行一次更具区分度的 fresh v2 brief | 可接受；没有把机械命中冒充语义闭合 |
| 直接复用 A33 的研究结论 | 重新扫描 dirty-but-pinned consumer、重新采集 LangGraph，并用 v2 ledger 重建 findings/Report IR | 可接受；第二次验收边界独立成立 |
| 完成技术报告后收口 | 额外执行 Map/Study、runtime/golden、quality、technical-report、plugin 和 diff gates | 可接受；报告与运行契约共同验收 |

**Drift 判定**: 可接受。第二次真实组合运行通过；证据不足的
`thread_id`/interrupt、Node/TS seam 和 host authority 仍显式保留为后续门，
没有被 `unknownCriteria: []` 静默改写。

### Concepts

本次没有新增领域词汇，复用了 `Repository Map`、`Architecture Study`、
`sealed ledger`、`Conformance fixture`、`Resume oracle`、`Native semantic
owner` 和 `Adapter-only reuse`。

### Actions

- ⚠️ fresh collection 完成后先检查 Evidence 是否集中在单一来源；机械
  `unknownCriteria` 命中不能替代语义 unknown review。

## 16:15 — Task: zj-roadmap-driven scope gate correction

**计划基线**: 无 `/zj-grilling` 产出 → drift 步骤跳过

### Concepts

- **Roadmap Scope Gate** — 在节点写入或项目文件修改前，先把 roadmap
  分类为 Product execution 或 Acceptance/evaluation；后者验证 skill/CLI/fixture，
  不自动授权目标项目开发。

该术语已写入 `ZJ-CONTEXT.md` 的 Roadmap 区域。

### Actions

- ⚠️ 执行 roadmap 节点前先确认交付物和授权范围；legacy/技能验收路线保持目标项目只读。
