# Retro — 2026-08-14

---

## 14:30 — Task: roadmap grilling Q3-Q5

**计划基线**: `/zj-grilling` 产出 — grilling 1-6-1 节点，预计 3 轮 Q&A 完成设计决策

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 3 轮 Q&A | 6 轮 Q&A | 用户在 Q6 产物落点上深入探讨了 A/B/C 三方案 + 追加 2 轮改进，决策质量更高但耗时翻倍 |
| 每轮 1 问 1 答 | Q6 出现多轮追问（B 的处理逻辑 → C 的优缺点 → C-refined → 改进建议） | grilling 模式的"一问一答"节奏在遇到设计空间探索时会被打破，这是合理的 |
| 预计 30 分钟 | 实际 ~50 分钟 | 偏差来自 Q6 的方案对比深度 |

**Drift 判定**: 可接受。Q6 的深度探索产生了 C-refined 这个更优方案，比原计划的"选 A 走人"质量高。但下次类似情况应在 grilling 阶段就把产物落点作为 Q 之一提出，而不是到 debrief 才发现这是个未决问题。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **闭环可见性** — 改进动作被写入后，下次开工时 agent 能否看到。是 retro 落点设计的核心约束。
- **时段互补** — grilling（前）与 debrief（后）在工作流时间轴上的互补关系。已同步到 ZJ-CONTEXT.md。
- **指针索引层** — ZJ-CONTEXT.md Retros 段只存路径+摘要，不存内容本身。内容下沉到 retro 文件。

### Actions

- ⚠️ 开工前先 grill 范围，避免中途发现关键决策点未覆盖
- ⚠️ 概念更新要及时，不要攒到 debrief 批量补（Rule 3 即时机制优先）

---

## 22:16 — Task: skills 安装脚本三平台通用化改造

**计划基线**: 无 `/zj-grilling` 产出（直接开工）→ drift 步骤跳过

### Drift

_跳过 — 未检测到 grilling 产出，无计划基线可对比_

### Concepts

调 `/zj-domain-modeling` 提取：

- **对账单模式** — `zagentic-skills-list` 作为安装/卸载的统一对账单，agent 参照它批量操作，不需要逐个找 skill 目录名。已同步到 ZJ-CONTEXT.md。

### Actions

- ⚠️ 备份目录必须落在安装目标目录**外**，否则隔离测试会数到备份里的副本
- ⚠️ dry-run 模式下不能有任何真实写入（包括 marker 文件）

---

## 21:00 — Task: 重命名 zj-setup-skills → zj-agents-init

**计划基线**: `/zj-grilling` 产出 — 6 轮 Q&A 定方案：目录 mv + frontmatter name 改 + 7 个消费 skill 联改 + README/ADR/ZJ-CONTEXT 同步 + 重生成 skills-list + commit/push + 本地 mv

### Drift

| 计划 | 实际 | 偏差 |
|------|------|------|
| 一次连续执行完成全部改动 | 2 次 429 限流中断，分 3 段续完 | 外部限流，非流程问题 |
| ZJ-CONTEXT.md 4 处引用 | 实际 5 处，1 处漏网靠收尾 grep 兜住补改 | 引用计数凭 grilling 时记忆，未先 grep 定数 |
| 本地同步 = mv 目录 | mv 后发现本地副本是旧版（frontmatter 仍 `name: zj-setup-skills`），须 cp 仓源覆盖 | 改名涉及 frontmatter name 时，mv 只改目录名，内容不同步 |

**Drift 判定**: 结果可接受（最终 commit `e3db414` 全 R100 rename 检出、引用清零），但两处执行偏差都指向同一根因：**改名前没有先 grep 出定量引用清单**，执行清单来自记忆而非文件系统。

### Concepts

调 `/zj-domain-modeling` 提取本次工作新出现的概念：

- **name-field coupling** — skill 目录名、frontmatter `name:`、调用名 `/zj-<name>` 三者必须一致；scanner 以 frontmatter name 路由，改名是三者联动而非只改目录。已同步到 ZJ-CONTEXT.md。
- **引用清零验证** — rename 后全仓 grep 旧名应为 0（append-only 历史日志除外），这是改名完成的客观判据，优于"改完了"的主观判断。

### Actions

- ⚠️ 改名前先 `grep -rn` 拿定量引用清单再执行，执行后 grep 清零验证——不要凭 grilling 记忆列文件
- ⚠️ skill 改名后本地同步必须以仓源覆盖（cp -r），只 mv 目录会让 frontmatter name 与目录名失配
